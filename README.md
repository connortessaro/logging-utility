# logging-utility
Structured Python logging utility that generates JSON-based logs, captures runtime execution metadata, and persists log entries to disk with full pytest coverage. Built to explore introspection, file-based logging, and testable system design.
